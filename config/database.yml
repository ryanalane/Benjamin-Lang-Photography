development:
  adapter: postgresql
  encoding: utf8
  database: blp_development
  pool: 5
  username: blp
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: blp_test
  pool: 5
  username: blp
  host: localhost
  min_messages: WARNING

production:
  adapter: postgresql
  encoding: utf8
  database: blp_production
